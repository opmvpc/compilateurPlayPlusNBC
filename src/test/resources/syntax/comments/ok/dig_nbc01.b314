
#import "world.map"

  int x;
  int y;
  bool b;
  char z;

void main()
{
  x = 0;
  y = 0;
  b = true;
  z = 'm';
 if(!b && (x > 42))
 {
    x = x + 2;
    left(x);
 }
 else{
    y = 1;
    down(y);
    y = y + 2;
    right(y);
    up(5);
 }

 dig();

 return void;
}