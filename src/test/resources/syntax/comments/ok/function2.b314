/* Test fonction  playPlus */

 #import "no.map"


 bool f1;
 bool fct1(int x)
 {
   f1 = true && (x < 5);
   left(2);
   return f1;
 }

 int f2;
 int  fct2(int x)
  {
    f2 = x + 5;
    up(2);
    return f2;
  }

  char f3;
  char fct3(bool x)
   {
     f3 = 'c';
     right(7);
     return f3;
   }

char f4;
 char  fct4()
  {
    f4 = '5';
    return f4;
  }

char b2[5] = {'a', 'f', 'n',  'k', 'p'};

 enum CAR {RED, BLUE, GREEN};

 typedef char CHARACTER;


 void main(){

    if (!fct1(5) == false) {
       up(6);
    }

    if (fct2(42) >= 40) {
        right(2);
     }

     if (!fct1(5) == false) {
        left(1);
     }

     if (fct1(2) != true) {
         down();
     }

    dig();

 return void;
 }
