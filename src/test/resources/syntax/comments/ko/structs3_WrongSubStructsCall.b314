/* Test fonction  playPlus */

#import "world.map"

    int z;
    int f;

int facto(int x)
{

 if(x < 0 || x == 0)
  {
   f = 1;
  }
  else {
   f = x * facto(x - 1);
  }

 return f;
}

bool b2[5] = {true, false, true, false, true};

enum NUMBER {ONE, TWO, THREE};

typedef int INTEGER;

  struct D1
        {
        int a[5];
        char b;
        bool c[5,3];
        };

struct  D2
        {
        int a[5];
        char b;
        bool c[5,3];
         struct D2 {
           char b;
           int c;
           bool d;
         };
        };


void main(){
   D2.D2.v;
   int x;

   x = 5;
   z = facto(x);

   if (!(z == (5*4*3*2*1))) {
       right(5);
   }

   dig();

return void;
}