#define turn_right(pwr,time) \
    OnRev(OUT_C,pwr) \
    wait time \
    OnFwd(OUT_B,pwr)
#define turn_left(pwr,time) \
    OnRev(OUT_B,pwr) \
    wait time \
    OnFwd(OUT_C,pwr)
#define up(pwr,time) \
    OnFwd(OUT_BC,pwr) \
    wait time
#define down(pwr,time) \
    OnRev(OUT_BC,pwr) \
    wait time"

thread main
	left(100,0)
	 PlayTone(TONE_C5,500)
	exit
endt
